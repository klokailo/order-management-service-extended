# Order Management Service

This repository houses the Order Management microservice. It is responsible for order lifecycle management, payment gateway webhooks, and inventory reservations.

## API Docs

Swagger UI available at `/docs` when the service is running.

Raw spec: `/docs/spec.json` (importable into Postman)

To regenerate the static spec file: `npx ts-node scripts/export-spec.ts`
